'use strict';
//attempt at a horizontal row
let row = new map([1, 2, 3, 4]);

let keyRow = [];
let keyMap = {};
let keyRowMap = function (x){
  return
}
// attempt at a horizontal column
let colum = new map([1, 2, 3, 4]);
let coordinates = .map'x, y';


//not sure yet
const boggle = function boggle() {};

const bigBoggle = function bigBoggle() {};

const superBigBoggle = function superBigBoggle() {};

module.exports = {
  boggle,
  bigBoggle,
  superBigBoggle,
  coordinates,
  map,

};
